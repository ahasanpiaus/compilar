#include <bits/stdc++.h>
#include <math.h>
using namespace std;
int main()
{
    int a,b,E;
    cout<<"Enter The Values:"<<endl;
    cin>>a; cin>>b;  E= pow(a,2)+2*a*b+ pow(b,2);
    cout<<E;
    }
